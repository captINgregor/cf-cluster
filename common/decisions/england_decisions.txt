operations = {
    ENG_home_guard = {
        icon = generic_army_support

        allowed = {
            original_tag = ENG
        }

        available = {
			any_state = {
				limit = {
					is_core_of = ENG
				}
				NOT = { is_controlled_by = ENg }
			}
        }

        fire_only_once = yes

        allowed = {
            ENG = {
                any_enemy_country = {
                    is_major = yes
                }
            }
        }

        complete_effect = {
            hidden_effect = {
                load_oob = ENG_home_guard
            }
        }
    }
}