operations = {
    ENG_home_guard = {
        icon = generic_army_support

        allowed = {
            original_tag = ENG
        }

        available = {
            custom_trigger_tooltip = { tooltip = ENG_not_activate_home_guard }
            hidden_trigger = {
                NOT = { has_country_flag = ENG_activated_home_guard }
            }
			any_state = {
				limit = {
					is_core_of = ENG
				}
				NOT = { is_controlled_by = ENg }
			}
        }

        fire_only_once = yes
        
        days_remove = 14

        visible = {
            ENG = {
                has_war = yes
            }
        }

        remove_effect = {
            custom_effect_tooltip = ENG_homeguard_tt
            hidden_effect = {
                load_oob = ENG_home_guard
                set_country_flag = ENG_activated_home_guard
            }
        }
    }
}